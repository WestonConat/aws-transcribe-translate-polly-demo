// ui.ts
export function updateUI(transcriptionResult: string) {
    // For testing, just log the result to the console
    console.log('[UI] updateUI called with transcriptionResult:', transcriptionResult);
  }
  